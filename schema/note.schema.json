{
  "$id": "note.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "description": "One agent note: durable findings written by LLM agents for LLM agents (audit results, discovered bugs, gotchas). Only this envelope is fixed - body accepts any JSON value and extra top-level fields are allowed, so each agent structures its memory as it sees fit. Humans only browse, search, archive, or delete. Canonical serialization applies. Filename must be notes/<id>.json.",
  "properties": {
    "author": {
      "minLength": 1,
      "type": "string"
    },
    "body": {},
    "created": {
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
      "type": "string"
    },
    "id": {
      "pattern": "^NOTE-[0-9]{8}-[a-z0-9]+(?:-[a-z0-9]+)*$",
      "type": "string"
    },
    "schema_version": {
      "const": 1
    },
    "status": {
      "enum": [
        "active",
        "archived"
      ]
    },
    "tags": {
      "items": {
        "minLength": 1,
        "type": "string"
      },
      "type": "array",
      "uniqueItems": true
    },
    "title": {
      "maxLength": 120,
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "schema_version",
    "id",
    "title",
    "created",
    "author",
    "status",
    "body"
  ],
  "title": "Agent note",
  "type": "object"
}
